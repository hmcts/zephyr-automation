describe('Login feature', () => {
  it('user logs in', {
    retries: 1,
    tags: ['@JIRA-KEY:ZE-1']
  }, () => {
    // implementation
  });

  it('user logs out', { tags: ['@REGRESSION'], retries: 0}, () => {
    // implementation
  });
});

