describe('Login feature', () => {
  it('user logs in', {
    retries: 1,
    tags: ['@JIRA-KEY:ZE-1', '@SMOKE']
  }, () => {
    // implementation
  });

  it('user logs out', {
    retries: 0
  }, () => {
    // implementation
  });
});

