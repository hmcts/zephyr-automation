#!groovy

@Library("Infrastructure")

def type = "java"
def product = "opal"
def component = "zephyr-automation"

withPipeline(type, product, component) {
    nonDeployableApp()
    releaseOnMerge()
}